Status: done

# Template Update all-Hosts mode and lineage reporting

## What to build

Extend Template Update with an explicit all-Hosts selection that updates every Host copy declaring the selected Template. Also add lineage reporting that lists existing VMs that declare the Template while making clear those durable VMs are not changed.

All-Hosts behavior must be intentional and visible at the command surface. There should be no implicit fleet-wide Template Update behavior.

## Acceptance criteria

- [x] Template Update supports an explicit all-Hosts selection for every Host that declares the selected Template.
- [x] Template Update does not update every Host implicitly from the default one-Host command path.
- [x] The workflow validates that at least one Host declares the selected Template before all-Hosts work begins.
- [x] Existing VMs that declare the Template are listed as lineage context.
- [x] Lineage output makes clear existing durable VMs are not changed by Template Update.
- [x] All-Hosts mode runs the same rebuild-and-verify behavior for each selected Host copy.
- [x] Tests prove all-Hosts selection, no implicit fleet-wide behavior, lineage output, and non-mutation of existing VMs and Services.

## Blocked by

- `.scratch/scoped-update-workflows/issues/06-template-update-one-host-copy.md`

## Triage Comment

> *This was generated by AI during triage.*

Completed Template Update all-Hosts mode and lineage reporting: added explicit `host=all` planning plus `just template-update-all`, routed Host scope and VM lineage through Inventory Entity Graph queries, reported durable VM lineage before execution, preserved the default one-Host behavior, validated no-declaring-Host selections before workflow commands, and covered all-host execution, no implicit fleet-wide update, lineage output, and VM/Service non-mutation with focused tests. Verified with focused Template Update and Entity Graph suites plus the full unittest suite.
