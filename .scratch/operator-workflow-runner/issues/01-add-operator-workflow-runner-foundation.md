> *This was generated by AI during architecture triage.*

Status: done
Category: enhancement

## What to build

Add the importable **Operator Workflow Runner** foundation around an inspectable **Operator Workflow Plan**.

The runner should execute ordered command phases and explicit confirmation gates, return structured workflow results, and standardize failure details without taking ownership of any workflow-specific ceremony rules. Existing workflow scripts may keep their current implementation in this slice, but the new module must be usable by later VM Lifecycle Convergence, Service Launch, and Host Readiness migrations.

## Acceptance criteria

- [x] A new importable module exposes an **Operator Workflow Plan** containing ordered command phases and distinct confirmation gates.
- [x] A command phase can declare a stable id, display name, command, diagnostic label, streaming preference, and failure policy.
- [x] A confirmation gate is a distinct plan step that requires exact operator input unless auto-confirm is enabled.
- [x] The runner executes plan steps in order and stops immediately on failed phases whose failure policy is stop.
- [x] The runner continues after failed phases whose failure policy is continue, records the failure, and returns a non-success workflow result.
- [x] Streaming command phases prefix console output by phase and retain a bounded output tail for diagnostics.
- [x] Non-streaming command phases capture stdout and stderr for diagnostics.
- [x] Each phase result includes status, command, return code, captured stdout/stderr where available, captured tail where available, and standardized failure detail.
- [x] Failure detail prefers stderr then stdout then `exit N` for non-streaming phases.
- [x] Failure detail prefers captured tail then stderr then `exit N` for streaming phases.
- [x] Fast unit tests cover successful execution, failed stop policy, failed continue policy, confirmation denial, auto-confirm, streaming prefix output, tail capture, and failure detail precedence.

## Blocked by

None - can start immediately

## Context

- `CONTEXT.md` defines **Operator Workflow Plan** and **Operator Workflow Runner**.
- The runner owns phase ordering, confirmation gates, subprocess execution, streaming prefix output, failure policy, structured results, and failure detail.
- The runner must not own **VM Lifecycle Convergence**, **Service Launch**, or **Host Readiness** ceremony rules.

## Notes for the agent

Keep the interface small and boring. The purpose is to concentrate workflow execution mechanics behind one seam, not to invent a general orchestration framework.

## Comments

> *This was generated by AI during triage.*

Completed and accepted. Added the importable Operator Workflow Runner foundation and verified it with:

- `python3 -m unittest tests.test_operator_workflow_runner`
- `python3 -m unittest tests.test_operator_workflow_runner tests.test_vm_up_workflow tests.test_service_launch_workflow tests.test_host_up_workflow`
- `just test`
