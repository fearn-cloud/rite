> *This was generated by AI during architecture deepening.*

# Human review: remove Service Runtime Intent fallback traversal

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to decide

Review the completed **Service Runtime Identity** migration and decide whether the transitional raw-yaml fallback traversal can be removed from Quadlet rendering and Service Deploy helpers. If approved, make **Service Runtime Intent** the only path for fortress-owned Service runtime meaning used by these adapters.

This is deliberately human-in-the-loop because it changes the maintenance contract: helper-level callers must provide or derive Service Runtime Intent instead of relying on adapters to reconstruct runtime meaning from raw Service yaml.

## Acceptance criteria

- [x] A human reviews the migration evidence from issue 11, including remaining fallback call sites and test coverage.
- [x] The human records approval, requested changes, or rejection in this issue's comments.
- [x] If approved, Quadlet rendering no longer derives Service Runtime Identity from raw Service yaml when rendering deployable Service artifacts.
- [x] If approved, Service Deploy helpers no longer derive Service Runtime Identity from raw Service yaml when building deploy variables.
- [x] Tests that previously relied on fallback traversal now build fleet-wide Service Runtime Intent or use focused Adapter fixtures that include resolved facts.
- [x] Error messages for missing runtime intent at adapter seams are explicit and use Service Runtime Intent vocabulary.
- [x] Full unit test suite passes after fallback removal.

## Blocked by

- `.scratch/service-runtime-intent/issues/11-runtime-identity-documentation-and-regression-sweep.md`

## Notes

This issue is the final gate for making Service Runtime Intent the only path. If the human rejects removal for a load-bearing reason, record that reason here and consider an ADR so future architecture reviews do not re-suggest the same removal.

## Comments

> *This was generated by AI during paired TDD implementation.*

### Review outcome - 2026-05-19

Decision: approved during paired review; remove the transitional raw-yaml fallback traversal and make **Service Runtime Intent** the only path for fortress-owned Service runtime meaning at Quadlet rendering, Service Deploy helper, Service Update, and VM reboot Service unit seams.

Implementation notes:

- `fortress_services/quadlet.py` now requires Service Runtime Intent for `render_quadlet_service` and `render_quadlet_container`.
- Quadlet runtime identity helpers now fail with explicit Service Runtime Intent errors when Service Network Identity, Container Identity, dependency systemd units, Service-owned Volume facts, or Share-backed Volume facts are missing, instead of deriving those facts from raw Service yaml.
- Quadlet Service Data Directories now come from Service Runtime Intent facts at the adapter seam.
- `fortress_services/deploy.py` now requires Service Runtime Intent for Share-backed Volume subpaths, Service Secret deploy facts, Native Service Environment Secret facts, Backend VM resolution, and direct Quadlet deploy variable construction unless a full model is supplied for fleet-wide analysis.
- Raw-yaml-derived `service_start_units` / `fortress_container_unit` helper fallback was removed; Service Update and VM reboot workflows now resolve Service unit order from Service Runtime Intent.
- Tests that directly exercise adapter behavior now pass focused Service Runtime Intent fixtures or analyze a small/full Inventory model before calling adapters.

Verification:

- `python3 -m unittest tests.test_service_quadlet_rendering` passed, 29 tests.
- `python3 -m unittest tests.test_service_deploy_workflow` passed, 28 tests.
- `python3 -m unittest tests.test_observability_config_generation` passed, 16 tests.
- `python3 -m unittest tests.test_service_update_workflow tests.test_vm_update_workflow tests.test_service_launch_workflow tests.test_service_group_launch_workflow` passed, 52 tests.
- `python3 -m unittest tests.test_new_service_runbook` passed, 6 tests.
- `python3 -m unittest discover` passed, 640 tests.
