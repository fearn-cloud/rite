> *This was generated by AI during triage.*

Status: done

# Bump Forgejo Inventory image to 15.0.3

## Agent Brief

**Category:** enhancement
**Summary:** Update the declared Forgejo container image from `11.0.1` to `15.0.3` and validate the repo before the live Service Update.

**Current behavior:**
Inventory currently declares the Forgejo Service container image as `codeberg.org/forgejo/forgejo:11.0.1`. The Forgejo Upgrade runbook identifies `15.0.3` as the target supported release for this explicit Upgrade.

**Desired behavior:**
Inventory should declare the Forgejo Service container image as `codeberg.org/forgejo/forgejo:15.0.3` and no other Forgejo runtime behavior should change. Local validation should pass before a human applies the Service Update in the maintenance window.

**Key interfaces:**
- Forgejo Service Inventory declaration - only the container image tag should change.
- Local validation command surface - the repo should pass the standard test command used before production changes.
- Forgejo Upgrade runbook - should remain aligned with the declared source and target image versions.

**Acceptance criteria:**
- [x] The Forgejo Service container image is `codeberg.org/forgejo/forgejo:15.0.3`.
- [x] No unrelated Forgejo Service fields change, including Backend VM, Ingress route, Published Ports, environment, Service Data Directory ownership, or volume mapping.
- [x] Any runbook test that asserts the declared source image is either updated intentionally or left passing for the correct post-change expectations.
- [x] `just test` passes locally, or any failure is recorded with enough detail to distinguish upgrade-related failures from unrelated existing failures.
- [x] The resulting diff is suitable to commit together with the Forgejo Upgrade runbook after live verification.

**Out of scope:**
- Running `just service-update forgejo`.
- Running live preflight, backup, migration, or post-upgrade verification.
- Moving to an intermediate Forgejo release unless the human-owned fallback path determines the direct upgrade failed.

## Comments

- 2026-07-08: Prerequisite gates are complete: `.scratch/forgejo-upgrade/issues/01-run-live-forgejo-preflight.md` and `.scratch/forgejo-upgrade/issues/02-take-forgejo-backup-gate.md` are both `done`.
- 2026-07-08: Bumped only `inventory/services/forgejo.yaml` container image from `codeberg.org/forgejo/forgejo:11.0.1` to `codeberg.org/forgejo/forgejo:15.0.3`. Added `tests/test_forgejo_inventory.py` to assert the inventory target image. Left the runbook source-image assertion intact because the runbook intentionally documents the `11.0.1` to `15.0.3` upgrade path. Validation passed with `python3 -m unittest tests.test_forgejo_inventory tests.test_forgejo_upgrade_runbook`, `just test`, and `python3 -m unittest discover`.

## Prerequisites

- [x] .scratch/forgejo-upgrade/issues/01-run-live-forgejo-preflight.md
- [x] .scratch/forgejo-upgrade/issues/02-take-forgejo-backup-gate.md
