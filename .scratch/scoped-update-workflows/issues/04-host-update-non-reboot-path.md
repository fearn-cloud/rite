Status: done

# Host Update non-reboot path

## What to build

Build the first complete Host Update path for one Host without reboot interruption handling. The workflow should run Host Configure first, then perform routine in-place Host software advancement within the Host's current compatibility band. Host Configure may report reboot-required state, but convergence must remain separate from maintenance-window reboot decisions.

Host Update should keep its target scope narrow: it should not rebuild Templates, update VMs, or update Services implicitly. Use Operator Workflow Plans for phase ordering, stop-on-failure behavior, streaming execution, and diagnostics.

## Acceptance criteria

- [x] Host Update rejects undeclared Hosts before mutation.
- [x] The Host Update plan runs Host Configure before software advancement.
- [x] Host Configure can report reboot-required state without rebooting as part of Host Configure.
- [x] Routine package advancement avoids removals and release transitions by default.
- [x] Host Update does not rebuild Templates or update VMs or Services implicitly.
- [x] A script wrapper and `just` target expose the workflow with an explicit Host argument.
- [x] Failures name the failed phase clearly.
- [x] Plan-builder and command-wrapper tests prove command ordering, validation failures, diagnostics, and runner integration.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

Completed Host Update non-reboot path. Added an Operator Workflow Plan that validates the Host, runs Host Configure first, then performs routine Host software advancement through `host-shell` using `apt-get update` and `apt-get upgrade --assume-yes --with-new-pkgs --no-remove`. Added the `scripts/host-update` wrapper, `just host-update`, focused plan-builder and wrapper tests for ordering, validation, narrow scope, diagnostics, and runner integration. Verified with `python3 -m unittest tests.test_host_update_workflow tests.test_host_configure_workflow tests.test_host_up_workflow tests.test_operator_workflow_runner`.
