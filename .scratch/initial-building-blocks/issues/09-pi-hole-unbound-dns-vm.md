Status: ready-for-human

## Parent

docs/prds/initial-building-blocks.md

## What to build

Pi-hole + Unbound deployed via the quadlet path on a dedicated VM, serving LAN DNS. The exact split (single container vs two) is implementation-time. DNS records for `*.fearn.cloud` come in slice 11 alongside the ingress regenerator.

## Acceptance criteria

- [ ] DNS VM declared in inventory and provisioned via the slice-6 path — inventory declaration is done; live provisioning remains
- [ ] Pi-hole + Unbound deployed via the slice-8 quadlet renderer — service declaration and local render proof are done; live deployment remains
- [ ] LAN clients can resolve external names through the VM
- [x] Architecture (single vs split container, upstream config) documented in `runbooks/dns-architecture.md`
- [ ] Demo: a LAN client configured with the new resolver successfully resolves both external and internal queries

## Blocked by

.scratch/initial-building-blocks/issues/07-quadlet-renderer-first-multi-container-service.md

## Comments

> *This was generated by AI during triage.*

## Human Brief

**Category:** enhancement
**Summary:** Complete and prove the Pi-hole + Unbound DNS VM as the LAN resolver.

**Current behavior:**
The declarative repo pieces are mostly present. `dns-primary-vm` is declared as an Infrastructure VLAN VM, `dns-primary` declares Pi-hole and Unbound via the Quadlet Service path, and the firewall matrix records the intended DNS VM placement and client resolution rules. The prior Quadlet renderer blocker is complete.

What is not yet proven from the issue is the live environment behavior: the DNS VM being provisioned and reachable on the LAN, LAN clients resolving through it, and the demo resolving both external names and internal names from an actual LAN client.

**Desired behavior:**
The operator can use the DNS VM as the LAN resolver. Pi-hole listens on TCP/UDP 53 at the declared Infrastructure VLAN address, forwards recursive queries to Unbound, and persists its Service-owned configuration under the standard Service Data Directory layout. The architecture choice for the Pi-hole/Unbound split and upstream path is documented in `runbooks/dns-architecture.md`.

**Key interfaces:**
- DNS VM inventory declaration — must remain aligned with the Homelab Firewall Matrix for placement, VLAN, address, and backup policy.
- Quadlet Service declaration for Pi-hole + Unbound — must remain a normal `deploy.type: quadlet` Service with pinned images and no ingress exposure.
- VM Lifecycle workflow — must successfully provision/configure the DNS VM through the standard `vm-up` path.
- Service Deploy workflow — must deploy/redeploy the DNS Service through the standard `service-deploy` path.
- DNS architecture runbook — should document the single-vs-split container decision, port binding, upstream/recursive configuration, persisted data paths, and expected LAN validation commands.

**Acceptance criteria:**
- [ ] DNS VM is provisioned from inventory through the standard VM Lifecycle path. Repo declaration exists; live `scripts/vm-up dns-primary-vm` proof remains.
- [ ] Pi-hole + Unbound are deployed through the standard Quadlet Service Deploy path. Service declaration and local Quadlet render proof exist; live `scripts/service-deploy dns-primary` proof remains.
- [ ] Pi-hole listens on both TCP and UDP port 53 at the declared DNS VM address.
- [ ] Pi-hole forwards upstream resolution through Unbound according to the documented architecture.
- [ ] A LAN client configured to use the DNS VM resolves an external hostname.
- [ ] A LAN client configured to use the DNS VM resolves an internal hostname expected for the current stage of the build.
- [x] `runbooks/dns-architecture.md` documents the architecture and operational validation steps.

## Progress

### 2026-05-11 static documentation slice

Completed the repo-local documentation portion with TDD:

- Added `runbooks/dns-architecture.md`.
- Documented the chosen two-container Quadlet Service split: `pihole` as the LAN-facing resolver and `unbound` as the recursive upstream.
- Documented `dns-primary-vm` placement at `10.40.0.11` on Infrastructure `VLAN 40`.
- Documented the standard operator paths: `scripts/vm-up dns-primary-vm` and `scripts/service-deploy dns-primary`.
- Documented the rendered Quadlet artifacts and systemd units:
  - `fortress-group-dns-primary.network`
  - `fortress-dns-primary-pihole.container`
  - `fortress-dns-primary-unbound.container`
  - `fortress-dns-primary-pihole.service`
  - `fortress-dns-primary-unbound.service`
- Documented persistent data paths:
  - `/srv/services/dns-primary/pihole/etc-pihole`
  - `/srv/services/dns-primary/unbound`
- Documented LAN validation commands for external and current-stage internal DNS lookups.
- Added `tests/test_dns_architecture_runbook.py` to keep the runbook from drifting away from the declared architecture.

Verification run:

- `python3 -m unittest tests.test_dns_architecture_runbook`
- `python3 -m unittest` passed with 298 tests.

Still open:

- Live VM provisioning through Proxmox.
- Live Service deployment to `dns-primary-vm`.
- Runtime proof that Pi-hole listens on TCP/UDP 53 at `10.40.0.11`.
- Runtime proof that Pi-hole forwards through Unbound.
- LAN client external and internal DNS resolution demo.

### 2026-05-11 acceptance criteria review

Repo-proven work:

- DNS VM declaration exists in `inventory/vms/dns-primary-vm.yaml`:
  - VM: `dns-primary-vm`
  - Address: `10.40.0.11/24`
  - VLAN: `40`
  - Host: `straylight`
  - Backup: enabled
- Pi-hole + Unbound Service declaration exists in `inventory/services/dns-primary.yaml`:
  - Service: `dns-primary`
  - Backend VM: `dns-primary-vm`
  - Deploy type: `quadlet`
  - Ingress: disabled
  - Containers: `pihole`, `unbound`
  - Pi-hole image: `docker.io/pihole/pihole:2025.05.0`
  - Unbound image: `docker.io/mvance/unbound:1.22.0`
  - Pi-hole depends on `unbound`
  - Pi-hole upstream: `FTLCONF_dns_upstreams: unbound#5335`
  - Pi-hole published DNS port: `10.40.0.11:53:53` with `protocol: tcp_udp`
- Local Quadlet render proof from the real `dns-primary` inventory produced:
  - `fortress-group-dns-primary.network`
  - `fortress-dns-primary-pihole.container`
  - `fortress-dns-primary-unbound.container`
  - `PublishPort=10.40.0.11:53:53/tcp`
  - `PublishPort=10.40.0.11:53:53/udp`
  - `Environment=FTLCONF_dns_upstreams=unbound#5335`
  - `Requires=fortress-dns-primary-unbound.service`
- Static tests covering the relevant area pass:
  - `python3 -m unittest tests.test_inventory_cross_file_validator tests.test_service_quadlet_rendering tests.test_dns_architecture_runbook`
  - `python3 -m unittest` passed with 298 tests.

Still requires operator/live infrastructure proof:

- Run `scripts/vm-up dns-primary-vm` against Proxmox and confirm the VM exists and is reachable at `10.40.0.11`.
- Run `scripts/service-deploy dns-primary` against `dns-primary-vm`.
- On `dns-primary-vm`, confirm the deployed units are active:
  - `fortress-dns-primary-pihole.service`
  - `fortress-dns-primary-unbound.service`
- On `dns-primary-vm`, confirm TCP and UDP listeners on `10.40.0.11:53`.
- From an allowed LAN client, confirm external DNS resolution with `dig @10.40.0.11 example.com A`.
- From an allowed LAN client, confirm the current-stage internal DNS record, expected to be checked with `dig @10.40.0.11 internal-ingress.fearn.cloud A` once that record is supposed to exist.
- Confirm Guest clients do not use the internal resolver.

**Why this is ready for human:**
The remaining proof depends on live LAN access, VM provisioning against Proxmox, router/firewall behavior, and client-side DNS configuration. An AFK agent can help refine docs or static configuration, but it cannot honestly complete the live resolver demo without operator-controlled infrastructure access.

**Out of scope:**
- Generating `*.fearn.cloud` Pi-hole records from Service inventory; that belongs with the ingress regenerator slice.
- Building the secondary DNS VM unless the maintainer explicitly expands this issue beyond the original primary DNS VM acceptance criteria.
- Changing router/firewall policy beyond validating that the DNS paths documented in the firewall matrix work.

> *This was generated by AI during triage.*

## Re-triage Notes

Re-evaluated on 2026-05-11 after the DNS runbook, inventory, and Quadlet render proof landed.

Recommendation: keep this issue `ready-for-human`, but narrow it mentally to the live acceptance proof for the primary DNS VM. The static/repo work is already complete enough that an agent should not keep re-implementing DNS architecture.

The remaining work is operator-controlled:

- prove `dns-primary-vm` is provisioned and reachable at `10.40.0.11`;
- deploy `dns-primary` to the VM with `scripts/service-deploy dns-primary`;
- verify the Pi-hole and Unbound systemd units are active;
- verify TCP and UDP listeners on `10.40.0.11:53`;
- verify external DNS from an allowed LAN client;
- defer generated `*.fearn.cloud` local records to issue 10, because ADR-0014 assigns local DNS record generation to `just ingress-regenerate`.

Issue 10 should no longer treat the whole DNS implementation as unstarted. Its dependency is now only the live DNS endpoint and Pi-hole record-push target.

### 2026-05-11 live acceptance workflow slice

Added a first-class operator workflow for the remaining live proof:

- `scripts/acceptance-dns-primary`
- `just acceptance-dns-primary`

The workflow now assumes `dns-primary-vm` already exists by default, deploys `dns-primary`, checks the Pi-hole and Unbound systemd units through `vm-shell`, verifies both TCP and UDP listeners on `10.40.0.11:53`, verifies Pi-hole is configured with `FTLCONF_dns_upstreams=unbound`, then runs an external DNS check against `10.40.0.11`. The later `*.fearn.cloud` internal lookup remains available as an explicit `internal=<hostname>` argument once issue 10 has generated Pi-hole local DNS records.

Use `just acceptance-dns-primary provision=true auto_confirm=true` only when the VM Lifecycle Convergence proof itself needs to be rerun.

Also updated `scripts/vm-shell` to support `scripts/vm-shell <vm> -- <command>...`, matching the DNS runbook's documented validation commands.

Verification run:

- `python3 -m unittest tests.test_acceptance_dns_primary_workflow tests.test_vm_shell_workflow tests.test_dns_architecture_runbook`
- `python3 -m unittest` passed with 302 tests.

The issue should remain `ready-for-human`: completing the acceptance criteria still requires running the workflow from the operator's live LAN/Proxmox environment and recording the result.

### 2026-05-11 acceptance-dns-primary bug pass

Fixed the live acceptance default so `just acceptance-dns-primary` proves the DNS endpoint that exists in this slice instead of failing on `internal-ingress.fearn.cloud`, which belongs to the ingress regenerator slice.

Verification run:

- `just acceptance-dns-primary` passed against `dns-primary-vm`
- `python3 -m unittest tests.test_acceptance_dns_primary_workflow tests.test_dns_architecture_runbook`

The internal lookup remains opt-in:

- `just acceptance-dns-primary internal=internal-ingress.fearn.cloud`
