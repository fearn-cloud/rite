Status: ready-for-human

## Parent

docs/prds/initial-building-blocks.md

## What to build

Proxmox Backup Server deployed in the inventory like any other VM. Datastore on a TrueNAS-hosted NFS export. Client-side encryption from day one (avoids re-encryption when off-site replication is added later). PBS encryption master key stored in the encrypted repo and at the offline backup location. Per-VM backup schedule and retention declared in the VM yaml with sensible global defaults.

## Acceptance criteria

- [ ] PBS VM declared in inventory, provisioned via slice-6 path — `inventory/vms/pbs-vm.yaml` exists; live provisioning remains
- [ ] PBS NFS datastore mounted via slice-7 path — `pbs-vm` declares the `pbs-datastore` Mount; live NAS/VM mount proof remains
- [ ] Client-side encryption enabled at PBS initialization
- [ ] Encryption master key generated as part of an operator ceremony in `runbooks/pbs.md`; stored both encrypted in repo and at offline backup location
- [ ] VM yaml schema supports `backup:` block (schedule, retention)
- [ ] Global backup defaults in `group_vars/all/backup.yaml`; per-VM block overrides
- [ ] Service data captured implicitly via VM-level snapshot (no parallel file-level backup tool)
- [ ] `runbooks/pbs.md` covers initial setup, encryption-key ceremony, restore drill
- [ ] Demo: scheduled backup of a test VM runs; restore succeeds

## Blocked by

.scratch/initial-building-blocks/issues/06-nfs-integration.md

## Comments

> *This was generated by AI during triage.*

## Re-triage Notes

**Category:** enhancement

Re-evaluated on 2026-05-11.

What is already repo-proven:

- `inventory/vms/pbs-vm.yaml` declares `pbs-vm` on `straylight` at `10.40.0.13`.
- `inventory/services/pbs.yaml` declares Proxmox Backup Server as a Native Service.
- `inventory/datasets/pbs-datastore.yaml` declares the NAS-backed datastore Dataset.
- `pbs-vm` declares an NFS Mount for `pbs-datastore`.
- `inventory/group_vars/all.yaml` declares the PBS apt repository.
- ADR-0015 still governs client-side encryption from day one.

What still needs to change:

- Add the missing operator runbook and ceremony for PBS initialization, client-side encryption key handling, and restore drill.
- Decide whether backup schedule/retention defaults belong in `inventory/group_vars/all.yaml` or a dedicated `group_vars/all/backup.yaml`; the current repo has a permissive `backup:` schema field on VMs but no visible global backup discipline workflow.
- Prove live provisioning, NFS datastore mounting, PBS initialization, encrypted backup, and restore with operator access.

Recommendation: keep this `ready-for-human` unless it is split. A good split would make schema/default/runbook work `ready-for-agent`, while the encrypted PBS initialization and restore demo remain human-operated.
