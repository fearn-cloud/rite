> *This was generated by AI during triage.*

Status: done

# Run live Forgejo preflight

## What to do

Work through the live preflight and breaking-change checklist for the Forgejo Upgrade before any Inventory image tag change is made. This issue is human-owned because it requires access to the live Forgejo VM, the Forgejo Site administration panel, and judgment about current Forgejo usage.

Use the Forgejo Upgrade runbook as the source of truth. Record the exact findings as comments on this issue so the later Inventory change and maintenance-window issues have a concrete gate to rely on.

## Acceptance criteria

- [x] The declared Service shape is confirmed: the Forgejo Service uses the `codeberg.org/forgejo/forgejo` image and runs on the Forgejo VM.
- [x] The live fortress-owned Forgejo unit is active before the upgrade attempt.
- [x] The live container image and `forgejo --version` output are recorded.
- [x] The Git version available to the live Forgejo container is recorded and satisfies Forgejo 13.0's minimum Git `>= 2.34.1` requirement.
- [x] The live `app.ini` location is recorded.
- [x] Removed or changed settings are checked and recorded, including `DISABLE_QUERY_AUTH_TOKEN`, `TEST_CONFLICTING_PATCHES_WITH_GIT_APPLY`, `CSRF_COOKIE_HTTP_ONLY`, `ADD_CO_COMMITTER_TRAILERS`, `SSH_ALLOW_UNEXPECTED_AUTHORIZED_KEYS`, and `COOKIE_REMEMBER_NAME`.
- [x] Customized templates, CSS, or public content are checked through the Forgejo Site administration panel's configuration tab, not `forgejo help`.
- [x] Current Forgejo Actions, runner, artifact URL, API client, public-only token, repository deletion, template generation, repository listing, admin hook, CLI-output parsing, SSH `authorized_keys`, forked `.profile`, squash-merge trailer, and avatar EXIF assumptions are checked or explicitly marked not used.
- [x] `forgejo doctor check --all` runs before the upgrade and its log path/result are recorded.
- [x] `forgejo manager flush-queues` completes before the upgrade; if it needs a larger timeout, the final timeout/result are recorded.

## Blocked by

None - can start immediately.

## Out of scope

- Changing the Forgejo image tag.
- Running `just service-update forgejo`.
- Taking or restoring production backups.
- Resolving unrelated Forgejo configuration drift unless it blocks this upgrade.

## Comments

### 2026-07-08 live preflight pass

Source-controlled declaration:

- `inventory/services/forgejo.yaml` declares Service `forgejo`, Backend VM `forgejo-vm`, Quadlet container `server`, image `codeberg.org/forgejo/forgejo:11.0.1`, HTTP port `3000`, SSH port `2222`, and data mounted at `/data`.
- `inventory/vms/forgejo-vm.yaml` declares `forgejo-vm` on Host `straylight`, VLAN 40, address `10.40.0.12/24`.

Live checks run from this workspace:

- `scripts/vm-shell forgejo-vm -- true`: succeeded.
- `systemctl is-active fortress-forgejo-server.service`: `active`.
- `sudo podman ps --filter name=fortress-forgejo-server --format ...`: container `fortress-forgejo-server`, image `codeberg.org/forgejo/forgejo:11.0.1`, status `Up 6 weeks`.
- `sudo podman exec fortress-forgejo-server forgejo --version`: `Forgejo version 11.0.1+gitea-1.22.0 (release name 11.0.1) built with GNU Make 4.4.1, go1.24.2 : bindata, timetzdata, sqlite, sqlite_unlock_notify`.
- `scripts/vm-shell forgejo-vm -- git --version`: failed with `git: command not found` on the VM host shell.
- `sudo podman exec fortress-forgejo-server git --version`: `git version 2.47.2`, satisfying Forgejo 13.0's minimum `>= 2.34.1` for the Git executable available inside the Forgejo container.
- Live `app.ini`: `/data/gitea/conf/app.ini`.
- Relevant removed/changed settings grep under `/data/gitea/conf` and `/data/forgejo/conf`: no matches for `DISABLE_QUERY_AUTH_TOKEN`, `TEST_CONFLICTING_PATCHES_WITH_GIT_APPLY`, `CSRF_COOKIE_HTTP_ONLY`, `ADD_CO_COMMITTER_TRAILERS`, `SSH_ALLOW_UNEXPECTED_AUTHORIZED_KEYS`, or `COOKIE_REMEMBER_NAME`.
- `sh -lc` inside the container hit `/etc/profile.d/gitea_bash_autocomplete.sh` syntax error; rerunning config probes with `sh -c` worked. The runbook was updated to run Forgejo maintenance CLI commands as container user `git`.
- `forgejo doctor check --all --log-file /tmp/doctor-before-upgrade.log` refused to run as root. Retried as `sudo podman exec --user git fortress-forgejo-server forgejo doctor check --all --log-file /tmp/doctor-before-upgrade.log`.
- Doctor result: command exited `0`, log path `/tmp/doctor-before-upgrade.log`, but reported `[E] authorized_keys file "/data/git/.ssh/authorized_keys" is out of date`; it recommends `forgejo admin regenerate keys` or `forgejo doctor check --run authorized-keys --fix`. It also warned that repository hook files are out of date across 10 repositories.
- `sudo podman exec --user git fortress-forgejo-server forgejo manager flush-queues --timeout 5m`: `Flushed`.

Live aggregate usage checks:

- SQLite database path from config: `/data/gitea/gitea.db`.
- Aggregate counts from live DB: `repository=10`, `access_token=0`, `action_runner=0`, `action_run=0`, `action_artifact=0`, `package_version=0`, `webhook=0`, `hook_task=0`, `push_mirror=3`, `pull_request=0`, `forgejo_auth_token=4`.
- Additional repository counts: `forked_profile_repos=0`, `profile_repos_total=0`, `template_repos=0`, `private_repos=0`, `repos_with_action_runs_counter=0`, `pull_requests=0`, `repo_avatars=0`.
- Repo search found no first-party Forgejo API client, runner, or token automation in this repository outside the Forgejo Service declaration and future-runner documentation.

### 2026-07-08 authorized_keys follow-up

Operator confirmed through the Forgejo Site administration panel that customized templates, CSS, and public content are good.

SSH `authorized_keys` was checked without modifying the live file:

- `/data/git/.ssh/authorized_keys` exists as `git:git`, mode `0600`, with 4 lines: 2 comments and 2 key entries.
- Forgejo DB `public_key` table has 2 rows: key IDs `2` and `3`.
- Fingerprints in `/data/git/.ssh/authorized_keys` match the DB exactly:
  - key `2`, `framework`: `SHA256:CwUMoYb7bc3Q5RFlnY1IAcszqD1d0+qwrIm+tI1cv78`
  - key `3`, `mfearn@brick-house`: `SHA256:u1Y6nkyqnAFOBdR7zz7Eibcml4s8ngID5JpLenI7FCQ`
- No extra/unexpected key entries were found in the managed `authorized_keys` file.
- The file is still stale according to `forgejo doctor check --run authorized-keys --log-file -`.
- The stale difference appears to be the forced command wrapper: entries use `/usr/local/bin/gitea --config=/data/gitea/conf/app.ini serv key-<id>`. In the current container `/usr/local/bin/forgejo` is a symlink to `/usr/local/bin/gitea`, and `/usr/local/bin/gitea` execs `/app/gitea/gitea`, so this is probably operationally harmless on 11.0.1 but should be regenerated before crossing Forgejo 14.0's stricter `authorized_keys` handling.

Recommended live fix before image change:

```sh
scripts/vm-shell forgejo-vm -- sudo podman exec --user git fortress-forgejo-server forgejo admin regenerate keys
scripts/vm-shell forgejo-vm -- sudo podman exec --user git fortress-forgejo-server forgejo doctor check --run authorized-keys --log-file -
```

### 2026-07-08 authorized_keys regeneration

Operator regenerated the Forgejo-managed SSH `authorized_keys` file and verified it with doctor:

```sh
scripts/vm-shell forgejo-vm -- sudo podman exec --user git fortress-forgejo-server forgejo admin regenerate keys
scripts/vm-shell forgejo-vm -- sudo podman exec --user git fortress-forgejo-server forgejo doctor check --run authorized-keys --log-file -
```

Result: `authorized-keys` doctor check is now OK. The stale `authorized_keys` pre-upgrade concern is resolved.

### 2026-07-08 operator usage confirmation

Operator confirmed current Forgejo usage:

- No Forgejo Actions.
- No runners.
- No artifacts.
- No API clients.
- No public-only tokens.
- The only non-standard configuration is that some repositories mirror to GitHub.

Together with the live DB counts and repository search above, the breaking-change usage assumptions are checked. The GitHub mirror usage should be re-smoked after upgrade by confirming mirrors still have remotes configured and no mirror-related errors appear in Forgejo.
