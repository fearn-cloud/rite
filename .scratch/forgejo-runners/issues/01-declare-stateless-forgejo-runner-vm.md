> *This was generated by AI from the Forgejo runners design session.*

# Declare the stateless Forgejo Runner VM

Status: ready-for-agent
Category: enhancement

## What to build

Declare the first Forgejo Runner VM in Inventory as a stateless, backup-excluded Infrastructure VLAN VM placed on the `neuromancer` Host. The declaration should make the VM demoable through the ordinary VM lifecycle path while preserving the design boundary that runner workloads do not run on the Forgejo VM and the Runner VM is rebuildable rather than restored from backup.

## Acceptance criteria

- [ ] Inventory declares one Forgejo Runner VM placed on the `neuromancer` Host.
- [ ] The Forgejo Runner VM is attached to the Infrastructure VLAN with a static address that does not conflict with existing address inventory.
- [ ] The Forgejo Runner VM is explicitly backup-excluded with a reason that matches its stateless recovery contract.
- [ ] The Forgejo Runner VM is not modeled as a Service and does not co-locate runner workloads on the Forgejo VM.
- [ ] Validation or tests prove the new VM declaration satisfies VM Inventory schema and existing inventory invariants.
- [ ] Operator-facing docs or inventory notes make rebuild plus VM Configure the recovery path for the Runner VM.

## Blocked by

None - can start immediately.
