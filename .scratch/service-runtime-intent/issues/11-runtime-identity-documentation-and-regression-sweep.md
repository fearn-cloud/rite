> *This was generated by AI during architecture deepening.*

# Runtime Identity documentation and regression sweep

Status: done
Category: enhancement
Date: 2026-05-19

## Parent

`.scratch/service-runtime-intent/PRD.md`

## What to build

Close out the transitional **Service Runtime Identity** migration with documentation alignment and a full regression sweep before the fallback path is removed. Confirm that **Service Runtime Intent** owns fortress-owned runtime identity while adapters still own rendered artifacts, Ansible variable names, systemd commands, and application-specific configuration.

## Acceptance criteria

- [x] `CONTEXT.md`, runbooks, and relevant docs use Service Runtime Intent and Service Runtime Identity consistently.
- [x] Documentation clearly preserves the distinction between runtime identity facts and rendered artifacts.
- [x] Existing Quadlet golden fixtures and Service Deploy workflow tests pass.
- [x] Direct Service Runtime Intent tests cover the deepest stable seam for Service Runtime Identity.
- [x] Adapter tests cover Quadlet rendering and Service Deploy translation from runtime facts.
- [x] The full unit test suite passes.
- [x] Any known remaining raw-yaml fallback traversal in Quadlet/deploy helpers is listed in this issue's comments for human review in the final issue.

## Blocked by

- `.scratch/service-runtime-intent/issues/09-quadlet-rendering-consumes-service-runtime-identity.md`
- `.scratch/service-runtime-intent/issues/10-service-deploy-consumes-service-runtime-identity.md`

## Notes

This issue should not remove fallback traversal. It prepares the evidence for the final human-in-the-loop removal.

## Comments

> *This was generated by AI during triage.*

### Regression sweep evidence - 2026-05-19

Documentation alignment:

- `CONTEXT.md` already names **Service Runtime Intent** as the owner of fortress-owned Service runtime meaning, including **Service Runtime Identity**, while keeping Quadlet files, Ansible variable names, systemd commands, Grafana JSON, and application-specific configuration with Adapters.
- `docs/architecture.md` now documents **Service Runtime Identity** as fortress-owned runtime names and paths, including Service Network names, Podman container names, systemd unit names, Service Secret names, Service Data Directory paths, and required mount unit names.
- `runbooks/new-service.md` now keeps Inventory identity, Service Runtime Identity, and Adapter output separate for new Service authors.

Known remaining transitional raw-yaml fallback traversal for issue 12 human review:

- `fortress_services/quadlet.py`: `_service_network_name`, `_container_runtime_name`, `_container_network_name`, `_container_alias`, and `_dependency_unit_name` still derive Service Network names, Podman container names, Container Alias values, and dependency systemd unit names from raw Service yaml when no Service Runtime Intent view is supplied.
- `fortress_services/quadlet.py`: `render_quadlet_container` still derives Share-backed Volume source paths, required mount units, Service-owned volume paths, container volume paths, and access modes from raw Service yaml and VM Mounts when runtime volume facts are absent.
- `fortress_services/quadlet.py`: `_service_secret_facts_for_container` still calls `service_secret_runtime_facts_for_service(service)` when no runtime intent is supplied.
- `fortress_services/quadlet.py`: `_service_data_directories`, `_service_owned_volume_source`, and `_needs_unbound_default_include_files` still traverse raw Service yaml for fallback Service Data Directory and generated file behavior.
- `fortress_services/deploy.py`: `share_backed_volume_subpaths`, `_unique_service_secret_facts`, `native_environment_secret_specs`, `native_environment_secret_keys`, `service_backend_vm_name`, `_runtime_service_data_directories`, `_service_start_units`, `_service_stop_units`, `service_start_units`, `fortress_container_unit`, and `_owned_quadlet_prune_paths` still have compatibility fallback paths that derive deploy facts or runtime identity from raw Service yaml when Service Runtime Intent is not supplied.
- `scripts/service-deploy`: the live deploy entrypoint already analyzes fleet-wide Service Runtime Intent and passes the per-Service view into Service Deploy and Quadlet helpers; the remaining fallback traversal is therefore helper-level compatibility for tests or direct callers.

Verification:

- `python3 -m unittest tests.test_architecture_docs.ArchitectureDocsTests.test_architecture_notes_describe_service_runtime_identity_seam` passed.
- `python3 -m unittest tests.test_new_service_runbook.NewServiceRunbookTests.test_runbook_documents_new_service_authoring_and_deployment` passed.
- `python3 -m unittest tests.test_architecture_docs tests.test_new_service_runbook` passed, 10 tests.
- `python3 -m unittest tests.test_service_runtime_intent` passed, 24 tests.
- `python3 -m unittest tests.test_service_quadlet_rendering` passed, 27 tests.
- `python3 -m unittest tests.test_service_deploy_workflow` passed, 26 tests.
- `python3 -m unittest discover` passed, 636 tests.
