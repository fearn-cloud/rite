Status: done

# Template Update for one Host copy

## What to build

Build the default Template Update workflow for one explicitly selected Host copy of one declared Template. The workflow should rebuild the selected Template from declared Template Inventory, replace the selected Host's existing Template copy through an explicit update path, and run Template Verification afterward.

This should reuse or extend the Template build and Template Verification machinery, but Template Update must not silently skip when the Template already exists. Existing VMs cloned from the Template are not changed.

## Acceptance criteria

- [x] Template Update requires explicit Host and Template selection.
- [x] Template Update rejects undeclared Hosts, undeclared Templates, and Host/Template pairs where the Host does not declare the Template.
- [x] The selected Host's Template copy is rebuilt from declared Template Inventory.
- [x] Existing Template copies are replaced through an explicit update path instead of being skipped as already present.
- [x] Template Verification runs after rebuild.
- [x] Failure output reports whether verification artifacts were preserved according to existing Template Verification behavior.
- [x] Existing VMs and Services are not interrupted or mutated by the default one-Host Template Update path.
- [x] A script wrapper and `just` target expose the workflow with explicit Host and Template arguments.
- [x] Plan-builder and command-wrapper tests prove replacement behavior, verification ordering, validation failures, diagnostics, and runner integration.

## Blocked by

None - can start immediately

## Triage Comment

> *This was generated by AI during triage.*

Completed Template Update for one selected Host copy: added the plan builder and script wrapper, exposed `just template-update`, extended `templates-build` with an explicit selected-template replacement path, and covered ordering, validation failures, diagnostics, runner integration, replacement behavior, verification artifact reporting, and non-mutation of VM/Service workflows. Verified with focused workflow tests and the full unittest suite.
