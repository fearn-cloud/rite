Status: done
Type: enhancement

## Parent

.scratch/initial-building-blocks/issues/05-tofu-yaml-to-resource-bridge-vm-lifecycle.md

## What to build

Write the new VM runbook and perform the real end-to-end demo on `wintermute` once the VM lifecycle slices are implemented. This issue is the acceptance/demo wrapper for the full split.

## Acceptance criteria

- [x] `runbooks/new-vm.md` documents adding `inventory/vms/<vm>.yaml`.
- [x] The runbook documents Prepare behavior, including refusal when the VM Sibling SOPS File already exists.
- [x] The runbook documents that the VM public key is plaintext in VM YAML and the private key is encrypted in the VM Sibling SOPS File.
- [x] The runbook documents `just vm-up vm=<name>` as the operator path.
- [x] The runbook documents the tofu plan approval flow.
- [x] The runbook documents Configure behavior and cloud-init waiting.
- [x] The runbook documents `just vm-destroy vm=<name>` and its Service Backend refusal.
- [x] The runbook states destroy removes only the provisioned Proxmox VM.
- [x] The runbook states destroy leaves `inventory/vms/<vm>.yaml` and `inventory/vms/<vm>.sops.yaml` in the repo.
- [x] The runbook documents when an AFK agent must stop and alert the maintainer for real Host access, destructive approval, or manual intervention.
- [x] Demo provisions a test VM end-to-end on `wintermute` through `just vm-up vm=<name>`.
- [x] Demo destroys the test VM cleanly through `just vm-destroy vm=<name>`.
- [x] Demo notes include exact command/output if maintainer intervention is needed.

## Blocked by

.scratch/initial-building-blocks/issues/05.6-vm-up-orchestration.md
.scratch/initial-building-blocks/issues/05.7-vm-destroy-workflow.md

## Design decisions

- This issue validates the full lifecycle after smaller tickets have landed.
- Real Host access and destructive approval are explicit maintainer checkpoints.

## Out of scope

- Implementing missing lifecycle code from earlier child tickets.
- Service deployment.
- Ingress regeneration.

## Comments

- 2026-05-04: Added `runbooks/new-vm.md` and `tests/test_new_vm_runbook.py`. Runbook acceptance is complete. Wintermute provision/destroy demo remains open because it requires real Host access and destructive approval from the maintainer.
- 2026-05-04: Ran the real wintermute lifecycle demo with `wintermute-demo` / VMID `8801`.
  - Provision command: `just vm-up vm=wintermute-demo`.
  - Approved plan summary: first successful full run planned replacement of `module.vms_wintermute.proxmox_virtual_environment_file.cloud_init_user_data["wintermute-demo"]` from user `fortress` to `admin`, plus creation of `module.vms_wintermute.proxmox_virtual_environment_vm.vm["wintermute-demo"]`; summary was `Plan: 2 to add, 0 to change, 1 to destroy.`
  - Apply result: `Apply complete! Resources: 2 added, 0 changed, 1 destroyed.`
  - Configure result: `wintermute-demo : ok=6 changed=1 unreachable=0 failed=0 skipped=0 rescued=0 ignored=0`.
  - Maintainer/manual intervention performed during demo: fixed missing `scripts/vm-prepare`; scoped selected-VM tofu generation and token decryption to the selected Host; formatted bpg/proxmox API token as `user!token_id=value`; excluded VM Sibling SOPS Files from tofu VM decoding; generated HTTPS/insecure provider config; supplied Host SSH private keys and a writable HOME to tofu; enabled snippets on wintermute with `mkdir -p /var/lib/vz/snippets; chmod 0755 /var/lib/vz/snippets; pvesm set local --content backup,import,iso,vztmpl,snippets`; changed VM cloud-init user generation to `local.globals.vm_admin_user`.
  - Destroy command: `just vm-destroy vm=wintermute-demo`.
  - Destroy result: `Destroy complete! Resources: 1 destroyed.`
  - Post-destroy verification on wintermute: `qm status 8801` returned `Configuration file 'nodes/wintermute/qemu-server/8801.conf' does not exist`; checks reported `vm-conf-absent` and `disk-absent`.
  - Repo verification after destroy: `inventory/vms/wintermute-demo.yaml` and `inventory/vms/wintermute-demo.sops.yaml` remain present.
