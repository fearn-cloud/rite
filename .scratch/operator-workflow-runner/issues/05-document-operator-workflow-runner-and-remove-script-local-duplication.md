> *This was generated by AI during architecture triage.*

Status: done
Category: enhancement

## What to build

Document the **Operator Workflow Runner** architecture and remove script-local workflow execution duplication left behind after the VM Lifecycle Convergence, Service Launch, and Host Readiness migrations.

This slice should make the new seam easy for future agents to discover and keep future workflow work from reintroducing local subprocess loops, local confirmation handling, or local failure-detail formatting.

## Acceptance criteria

- [x] Architecture or agent-facing documentation explains when to use an **Operator Workflow Plan** and **Operator Workflow Runner**.
- [x] Documentation states that plan builders own domain-specific ceremony rules and the runner owns execution mechanics.
- [x] Documentation calls out distinct confirmation gates, stop versus continue failure policy, streaming prefix output, captured tails, and standardized failure detail.
- [x] VM Lifecycle Convergence, Service Launch, and Host Readiness scripts no longer contain duplicate local `run_phase` or `phase_detail` implementations for behavior now owned by the runner.
- [x] Tests or architecture-doc checks protect the intended module names and responsibilities enough for future agents to find the seam.
- [x] Existing workflow tests still pass after removing duplication.

## Blocked by

- `.scratch/operator-workflow-runner/issues/02-migrate-vm-lifecycle-convergence-to-workflow-runner.md`
- `.scratch/operator-workflow-runner/issues/03-migrate-service-launch-to-workflow-runner.md`
- `.scratch/operator-workflow-runner/issues/04-migrate-host-readiness-to-workflow-runner.md`

## Context

- `CONTEXT.md` already names **Operator Workflow Plan** and **Operator Workflow Runner**.
- The deletion test for this architecture only passes if workflow execution mechanics stay concentrated behind the runner after migration.

## Notes for the agent

This is the cleanup and signposting issue. Avoid broad refactors outside the migrated workflows unless a duplicated execution helper is directly competing with the new runner seam.

## Comments

> *This was generated by AI during triage.*

Completed. Added architecture documentation for Operator Workflow Plan versus Operator Workflow Runner, including the plan-builder/runner responsibility split, confirmation gates, failure policy, streaming prefix output, captured tails, and standardized failure detail. Added an architecture docs test that protects the module names and verifies `scripts/vm-up`, `scripts/service-launch`, and `scripts/host-up` do not reintroduce local `run_phase` or `phase_detail` helpers.

Tests run:

- `python3 -m unittest tests.test_architecture_docs.ArchitectureDocsTests.test_architecture_notes_describe_operator_workflow_runner_seam`
- `python3 -m unittest tests.test_architecture_docs tests.test_operator_workflow_runner tests.test_vm_up_workflow tests.test_service_launch_workflow tests.test_host_up_workflow tests.test_host_readiness_plan`
